// Coloque aqui suas actions

const addUser = () => ({ type: 'ADD_USER', payload });

const addRecord = () => ({ type: 'ADD_RECORD', payload });

const delRecord = () => ({ type: 'DEL_RECORD', payload });

const editRecord = () = ({ type: 'EDIT_RECORD', payload });
